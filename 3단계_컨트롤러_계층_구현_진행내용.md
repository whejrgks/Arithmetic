# 3단계: 컨트롤러 계층 구현 진행 내용

## 개요

3단계에서는 CalculatorController 클래스를 완성하고, 모든 계산기 로직을 구현하여 상태 관리, 연산 실행, 예외 처리를 담당하는 컨트롤러 계층을 완성했습니다.

---

## 3.1 CalculatorController 클래스 생성

### 책임

CalculatorController는 다음 책임을 가집니다:

1. **계산기 상태 관리**
   - 현재 값, 이전 값, 연산자, 입력 모드 관리
   
2. **연산 실행 및 결과 계산**
   - Strategy 패턴을 사용한 연산 실행
   - 결과 포맷팅 및 디스플레이 업데이트
   
3. **예외 처리 및 에러 상태 관리**
   - 0으로 나누기 등 산술 오류 처리
   - 에러 상태 복구 메커니즘

### 상태 관리 변수

```python
self._current_value: str = "0"              # 현재 입력/표시 값
self._previous_value: Optional[float] = None # 이전 값
self._operator: Optional[str] = None        # 선택된 연산자
self._waiting_for_operand: bool = False     # 새 피연산자 입력 대기 여부
self._display_callback: Optional[Callable]   # 디스플레이 업데이트 콜백
```

### 구현 완료 상태

- ✅ 상태 관리 변수 정의
- ✅ 의존성 주입 지원 (Arithmetic 인스턴스 주입 가능)
- ✅ 디스플레이 콜백 메커니즘
- ✅ 클래스 문서화 및 타입 힌팅

---

## 3.2 계산기 로직 구현

### 1. 숫자 입력 처리

**메서드**: `input_number(digit: str)`

**기능**:
- 한 자리 숫자(0-9) 입력 처리
- 여러 자리 숫자 구성 지원
- 에러 상태 자동 복구
- 연산자 입력 후 새 숫자 입력 처리

**구현 특징**:
```python
def input_number(self, digit: str) -> None:
    # 에러 상태일 경우 Clear 처리
    if self._current_value == "Error":
        self.clear()
        self._current_value = digit
        self._update_display()
        return
    
    # 숫자 검증
    if not digit.isdigit() or len(digit) != 1:
        return
    
    if self._waiting_for_operand:
        self._current_value = digit
        self._waiting_for_operand = False
    else:
        if self._current_value == "0":
            self._current_value = digit
        else:
            self._current_value += digit
    self._update_display()
```

**테스트 결과**: ✅ 모든 테스트 통과
- 기본 숫자 입력
- 여러 자리 숫자 입력
- 연산자 후 숫자 입력
- 계산 후 숫자 입력

---

### 2. 연산자 선택 처리

**메서드**: `set_operator(operator: str)`

**기능**:
- 연산자 설정 (+, -, ×, /)
- 연속 연산자 변경 시 이전 연산 자동 계산
- 에러 상태 처리
- 지원하지 않는 연산자 무시

**구현 특징**:
```python
def set_operator(self, operator: str) -> None:
    # 에러 상태일 경우 무시
    if self._current_value == "Error":
        return
    
    if not OperationFactory.is_supported(operator):
        return
    
    # 이전 연산이 있고, 피연산자를 기다리는 상태가 아니면 먼저 계산
    if self._operator is not None and not self._waiting_for_operand:
        self.calculate()
        if self._current_value == "Error":
            return
    
    try:
        self._previous_value = float(self._current_value)
        self._operator = operator
        self._waiting_for_operand = True
    except (ValueError, OverflowError):
        self._current_value = "Error"
        self._update_display()
```

**테스트 결과**: ✅ 모든 테스트 통과
- 각 연산자 설정
- 연속 연산자 변경
- 연산자 체인 처리

---

### 3. 등호(=) 처리

**메서드**: `calculate()`

**기능**:
- 현재 연산 실행
- Strategy 패턴을 사용한 연산 수행
- 결과 포맷팅 (정수/소수점 처리)
- 오버플로우 체크
- 예외 처리

**구현 특징**:
```python
def calculate(self) -> None:
    if self._operator is None or self._previous_value is None:
        return
    
    try:
        current = float(self._current_value)
        operation = OperationFactory.create(self._operator)
        
        if operation is None:
            self._current_value = "Error"
            self._update_display()
            return
        
        # Strategy 패턴을 사용하여 연산 실행
        result = operation.execute(self._previous_value, current)
        
        # 결과를 문자열로 변환 (정수면 정수로, 소수면 소수로)
        if abs(result) > 1e15:
            self._current_value = "Error"
        elif result == int(result):
            self._current_value = str(int(result))
        else:
            # 소수점 이하 불필요한 0 제거
            self._current_value = str(result).rstrip('0').rstrip('.')
            if not self._current_value or self._current_value == "-":
                self._current_value = "0"
        
        self._operator = None
        self._previous_value = None
        self._waiting_for_operand = True
        self._update_display()
        
    except ArithmeticError:
        self._current_value = "Error"
        self._operator = None
        self._previous_value = None
        self._waiting_for_operand = True
        self._update_display()
    except Exception:
        self._current_value = "Error"
        self._update_display()
```

**테스트 결과**: ✅ 모든 테스트 통과
- 덧셈, 뺄셈, 곱셈, 나눗셈 계산
- 소수점 결과 처리
- 0으로 나누기 예외 처리
- 연산자 없이 계산 시도 (무시)

---

### 4. 초기화(Clear) 처리

**메서드**: `clear()`

**기능**:
- 모든 상태 초기화
- 디스플레이를 "0"으로 리셋
- 에러 상태 복구

**구현 특징**:
```python
def clear(self) -> None:
    """계산기를 초기화합니다."""
    self._current_value = "0"
    self._previous_value = None
    self._operator = None
    self._waiting_for_operand = False
    self._update_display()
```

**테스트 결과**: ✅ 모든 테스트 통과
- 기본 Clear 기능
- 에러 후 Clear 복구

---

### 5. 부호 변경(+/-) 처리

**메서드**: `toggle_sign()`

**기능**:
- 현재 값의 부호 변경
- 0은 변경하지 않음
- 에러 상태 무시

**구현 특징**:
```python
def toggle_sign(self) -> None:
    # 에러 상태일 경우 무시
    if self._current_value == "Error":
        return
    
    if self._current_value != "0" and self._current_value != "0.":
        if self._current_value.startswith("-"):
            self._current_value = self._current_value[1:]
        else:
            self._current_value = "-" + self._current_value
        self._update_display()
```

**테스트 결과**: ✅ 모든 테스트 통과
- 양수 → 음수 변경
- 음수 → 양수 변경
- 0의 부호 변경 (변경되지 않음)
- 계산 결과의 부호 변경

---

### 6. 소수점 처리

**메서드**: `input_decimal()`

**기능**:
- 소수점 입력 처리
- 중복 소수점 입력 방지
- 연산자 후 소수점 입력 처리
- 에러 상태 자동 복구

**구현 특징**:
```python
def input_decimal(self) -> None:
    # 에러 상태일 경우 Clear 처리
    if self._current_value == "Error":
        self.clear()
        self._current_value = "0."
        self._update_display()
        return
    
    if self._waiting_for_operand:
        self._current_value = "0."
        self._waiting_for_operand = False
    elif "." not in self._current_value:
        self._current_value += "."
    self._update_display()
```

**테스트 결과**: ✅ 모든 테스트 통과
- 기본 소수점 입력
- 연산자 후 소수점 입력
- 중복 소수점 입력 방지
- 소수점 결과 계산

---

## 테스트 결과

### 전체 테스트 통과

```bash
python -m pytest tests/test_calculator_controller.py -v
```

**결과**: ✅ **30개 테스트 모두 통과**

```
tests/test_calculator_controller.py::TestCalculatorController::test_input_number_basic PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_input_number_multiple_digits PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_input_number_after_operator PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_input_number_after_calculate PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_set_operator_add PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_set_operator_subtract PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_set_operator_multiply PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_set_operator_divide PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_set_operator_chain PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_add PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_subtract PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_multiply PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_divide PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_divide_decimal PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_divide_by_zero PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_without_operator PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_clear PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_clear_after_error PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_toggle_sign_positive_to_negative PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_toggle_sign_negative_to_positive PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_toggle_sign_zero PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_toggle_sign_after_calculate PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_input_decimal PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_input_decimal_after_operator PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_input_decimal_duplicate PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_calculate_decimal_result PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_complex_calculation PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_continuous_operator_change PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_error_recovery PASSED
tests/test_calculator_controller.py::TestCalculatorController::test_display_callback PASSED

============================= 30 passed in 0.08s ==============================
```

### 테스트 커버리지

- ✅ **숫자 입력 처리**: 4개 테스트
- ✅ **연산자 선택 처리**: 5개 테스트
- ✅ **등호(=) 처리**: 6개 테스트
- ✅ **초기화(Clear) 처리**: 2개 테스트
- ✅ **부호 변경(+/-) 처리**: 4개 테스트
- ✅ **소수점 처리**: 4개 테스트
- ✅ **복합 시나리오**: 3개 테스트
- ✅ **에러 처리**: 2개 테스트

---

## 개선 사항

### 1. 에러 상태 관리 개선

**개선 전**:
- 에러 발생 시 단순히 "Error" 표시
- 에러 상태 복구 메커니즘 부족

**개선 후**:
- 모든 입력 메서드에서 에러 상태 자동 감지
- Clear 또는 새 입력 시 자동 복구
- 에러 상태에서 연산자 설정 방지

### 2. 숫자 입력 검증 강화

**추가 기능**:
- 숫자 문자열 검증 (isdigit, 길이 체크)
- 잘못된 입력 무시

### 3. 소수점 처리 개선

**개선 사항**:
- 중복 소수점 입력 방지
- "0." 같은 경우 처리
- 연산자 후 소수점 입력 시 "0."으로 시작

### 4. 결과 포맷팅 개선

**개선 사항**:
- 정수 결과는 정수로 표시
- 소수점 이하 불필요한 0 제거
- 오버플로우 체크 (1e15 초과 시 에러)

### 5. 연속 연산 처리 개선

**개선 사항**:
- 연속 연산자 변경 시 이전 연산 자동 계산
- 연산자 체인 처리 개선

---

## 생성/수정된 파일 목록

### 새로 생성된 파일
1. `tests/test_calculator_controller.py` - CalculatorController 테스트 코드 (30개 테스트)

### 수정된 파일
1. `src/calculator_controller.py` - 기능 개선 및 에러 처리 강화

---

## SOLID 원칙 준수 확인

### ✅ Single Responsibility 원칙
- CalculatorController는 계산기 상태 관리와 연산 실행만 담당
- UI 로직과 비즈니스 로직 분리

### ✅ Dependency Inversion 원칙
- Arithmetic 인스턴스를 의존성 주입으로 받음
- 테스트 시 Mock 객체 주입 가능

### ✅ Open/Closed 원칙
- Strategy 패턴을 통해 새로운 연산자 추가 시 기존 코드 수정 불필요

---

## 주요 기능 검증

### 기본 연산
- ✅ 덧셈: 5 + 3 = 8
- ✅ 뺄셈: 10 - 3 = 7
- ✅ 곱셈: 5 × 3 = 15
- ✅ 나눗셈: 10 / 2 = 5

### 소수점 연산
- ✅ 소수점 입력: 5.2
- ✅ 소수점 결과: 5 / 2 = 2.5

### 에러 처리
- ✅ 0으로 나누기: 5 / 0 = Error
- ✅ 에러 복구: Clear 후 정상 계산 가능

### 복합 시나리오
- ✅ 연속 계산: 10 + 5 - 3 × 2 / 4 = 6
- ✅ 연속 연산자 변경: 10 + → - → × 2 = 20

---

## 다음 단계

3단계가 완료되었으므로 다음 단계로 진행할 수 있습니다:

- **4단계**: PyQt GUI 구현 (이미 1단계에서 기본 구조 완료, 추가 개선 가능)
- **5단계**: 리소스 및 설정 분리
- **6단계**: 예외 처리 및 에러 핸들링 (일부 완료, 추가 개선 가능)
- **7단계**: 테스트 가능성 개선 (완료)

---

## 요약

✅ **3.1 CalculatorController 클래스 생성**: 완료
- 상태 관리 변수 정의
- 의존성 주입 지원
- 디스플레이 콜백 메커니즘

✅ **3.2 계산기 로직 구현**: 완료
- 숫자 입력 처리 ✅
- 연산자 선택 처리 ✅
- 등호(=) 처리 ✅
- 초기화(Clear) 처리 ✅
- 부호 변경(+/-) 처리 ✅
- 소수점 처리 ✅

✅ **테스트 코드 작성**: 30개 테스트 모두 통과
✅ **에러 처리 개선**: 에러 상태 관리 및 복구 메커니즘 구현
✅ **코드 품질 개선**: 타입 힌팅, 문서화, 검증 로직 추가

