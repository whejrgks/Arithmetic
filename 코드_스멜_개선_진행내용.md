# 코드 스멜 개선 진행내용

## 개요

리팩토링 코드 스멜 분석 문서(`리팩토링_코드_스멜_분석.md`)에서 식별된 코드 스멜들을 단계적으로 개선한 내용을 정리했습니다.

---

## 1. calculator_ui.py 개선

### 1.1 중복 코드 (Duplicate Code) ✅

**문제점**:
- 버튼 스타일 설정 코드가 반복됨
- 버튼 생성 로직이 유사하게 반복됨

**개선 내용**:
- `_create_button()` 헬퍼 메서드 추출
- 버튼 생성 로직을 단일 메서드로 통합
- 스타일 적용 로직을 `CalculatorStyles` 클래스로 분리

**변경 파일**: `src/calculator_ui.py`

**개선 효과**:
- 버튼 생성 코드 중복 제거
- 버튼 스타일 변경 시 한 곳만 수정하면 됨

---

### 1.2 긴 메서드 (Long Method) ✅

**문제점**:
- `_init_ui()` 메서드가 약 140줄로 너무 김
- UI 초기화, 버튼 생성, 스타일 적용, 이벤트 연결이 모두 한 메서드에 있음

**개선 내용**:
- `_setup_window()`: 윈도우 기본 설정
- `_setup_main_layout()`: 메인 레이아웃 설정
- `_create_display()`: 디스플레이 생성
- `_create_button_grid()`: 버튼 그리드 생성
- `_create_button()`: 개별 버튼 생성
- `_connect_button_signal()`: 버튼 시그널 연결

**변경 파일**: `src/calculator_ui.py`

**개선 효과**:
- 각 메서드가 단일 책임을 가짐
- 코드 가독성 향상
- 유지보수 용이

---

### 1.3 매직 스트링 (Magic String) ✅

**문제점**:
- 하드코딩된 색상 코드, 폰트 이름, 스타일 문자열

**개선 내용**:
- `src/resources/styles.py` 파일 생성
- `CalculatorStyles` 클래스에 모든 UI 상수 정의:
  - 색상 상수 (`COLOR_BACKGROUND`, `COLOR_NUMBER_BUTTON`, 등)
  - 폰트 상수 (`FONT_FAMILY`, `FONT_SIZE_DISPLAY`, `FONT_SIZE_BUTTON`)
  - 크기 상수 (`WINDOW_WIDTH`, `WINDOW_HEIGHT`, `BUTTON_MIN_HEIGHT`)
  - 스타일 템플릿 (`STYLE_BUTTON_BASE`, `STYLE_BUTTON_BOLD`)
- 스타일 생성 메서드 추가:
  - `get_display_style()`
  - `get_window_style()`
  - `get_button_style(style_type)`

**변경 파일**: 
- `src/calculator_ui.py`
- `src/resources/styles.py` (신규 생성)

**개선 효과**:
- 매직 스트링 제거
- UI 스타일 변경 시 한 곳만 수정
- 코드 가독성 향상

---

### 1.4 중복 호출 (Duplicate Call) ✅

**문제점**:
- `__init__()` 메서드에서 `_setup_shortcuts()`가 두 번 호출됨

**개선 내용**:
- 중복된 `_setup_shortcuts()` 호출 제거

**변경 파일**: `src/calculator_ui.py`

**개선 효과**:
- 불필요한 중복 호출 제거
- 성능 개선 (미미하지만)

---

### 1.5 하드코딩된 값 (Hard-coded Values) ✅

**문제점**:
- 윈도우 크기, 폰트 크기, 버튼 높이 등이 하드코딩됨

**개선 내용**:
- 모든 하드코딩된 값을 `CalculatorStyles` 클래스의 상수로 이동
- `WINDOW_WIDTH = 320`
- `WINDOW_HEIGHT = 450`
- `FONT_SIZE_DISPLAY = 24`
- `FONT_SIZE_BUTTON = 14`
- `BUTTON_MIN_HEIGHT = 50`

**변경 파일**: 
- `src/calculator_ui.py`
- `src/resources/styles.py`

**개선 효과**:
- 매직 넘버 제거
- 설정 변경 용이

---

### 1.6 Feature Envy ✅

**문제점**:
- UI 클래스가 컨트롤러의 내부 동작을 너무 많이 알고 있음
- "−" 기호를 "-"로 변환하는 로직이 UI에 있음

**개선 내용**:
- 연산자 기호 정규화 로직을 `CalculatorController`의 `_normalize_operator()` 메서드로 이동
- UI는 표시용 기호를 그대로 전달하고, 컨트롤러에서 내부 기호로 변환

**변경 파일**: 
- `src/calculator_ui.py`
- `src/calculator_controller.py`

**개선 효과**:
- 관심사 분리 개선
- UI와 컨트롤러 간 결합도 감소

---

## 2. calculator_controller.py 개선

### 2.1 매직 스트링 (Magic String) ✅

**문제점**:
- "Error" 문자열이 여러 곳에서 하드코딩됨

**개선 내용**:
- 클래스 상수로 정의: `ERROR_STATE = "Error"`
- 모든 "Error" 문자열을 `self.ERROR_STATE`로 변경

**변경 파일**: `src/calculator_controller.py`

**개선 효과**:
- 매직 스트링 제거
- 에러 상태 문자열 변경 시 한 곳만 수정

---

### 2.2 중복 코드 (Duplicate Code) ✅

**문제점**:
- 에러 상태 체크 및 처리 로직이 반복됨

**개선 내용**:
- `_is_error_state()`: 에러 상태 확인 헬퍼 메서드
- `_set_error_state(reset_operation=False)`: 에러 상태 설정 메서드
- `_handle_error_state_for_input(digit)`: 에러 상태에서 숫자 입력 처리
- `_handle_error_state_for_decimal()`: 에러 상태에서 소수점 입력 처리

**변경 파일**: `src/calculator_controller.py`

**개선 효과**:
- 중복 코드 제거
- 에러 처리 로직 일관성 향상

---

### 2.3 매직 넘버 (Magic Number) ✅

**문제점**:
- `1e15`가 무엇을 의미하는지 불명확

**개선 내용**:
- 클래스 상수로 정의: `MAX_DISPLAY_VALUE = 1e15`
- 오버플로우 체크 로직에서 상수 사용

**변경 파일**: `src/calculator_controller.py`

**개선 효과**:
- 매직 넘버 제거
- 코드 의도 명확화

---

### 2.4 긴 메서드 (Long Method) ✅

**문제점**:
- `calculate()` 메서드가 약 45줄로 복잡함
- 계산, 결과 포맷팅, 에러 처리, 상태 업데이트가 모두 한 메서드에 있음

**개선 내용**:
- `_format_result(result)`: 결과 포맷팅 로직 분리
- `_reset_operation_state()`: 연산 상태 초기화 로직 분리
- `_set_error_state()`: 에러 상태 설정 로직 분리

**변경 파일**: `src/calculator_controller.py`

**개선 효과**:
- 메서드 책임 분리
- 코드 가독성 향상
- 단위 테스트 용이

---

### 2.5 예외 처리 중복 (Exception Handling Duplication) ✅

**문제점**:
- 에러 상태 설정 로직이 여러 예외 처리 블록에서 중복됨

**개선 내용**:
- `_set_error_state(reset_operation=False)` 메서드로 통합
- 모든 예외 처리에서 `_set_error_state()` 호출

**변경 파일**: `src/calculator_controller.py`

**개선 효과**:
- 예외 처리 로직 중복 제거
- 에러 처리 일관성 향상

---

### 2.6 결과 포맷팅 로직 복잡도 ✅

**문제점**:
- 결과를 문자열로 변환하는 로직이 복잡함
- 소수점 처리 로직이 까다로움

**개선 내용**:
- `_format_result(result)` 메서드로 분리
- 오버플로우 체크, 정수/소수점 처리, 불필요한 0 제거 로직 포함

**변경 파일**: `src/calculator_controller.py`

**개선 효과**:
- 포맷팅 로직 분리
- 테스트 용이
- 유지보수 용이

---

## 3. arithmetic.py 개선

### 3.1 불필요한 객체 생성 (Unnecessary Object Creation) ✅

**문제점**:
- Strategy 객체를 미리 생성하여 저장
- 사용하지 않을 수도 있는 객체를 미리 생성

**개선 내용**:
- Lazy Initialization 패턴 적용
- 각 연산별 `_get_*_operation()` 메서드 추가
- 필요할 때만 Strategy 객체 생성
- `_get_add_operation()`, `_get_subtract_operation()`, `_get_multiply_operation()`, `_get_divide_operation()`, `_get_divide_integer_operation()`

**변경 파일**: `src/arithmetic.py`

**개선 효과**:
- 불필요한 객체 생성 방지
- 메모리 사용량 감소
- 초기화 시간 단축

---

### 3.2 Wrapper 클래스 (Thin Wrapper)

**상태**: 유지 (의도적 설계)

**설명**:
- `Arithmetic` 클래스는 기존 API를 유지하기 위한 래퍼 역할
- 기존 코드와의 호환성을 위해 현재 구조 유지
- Lazy Initialization으로 성능 개선 완료

---

## 4. operations/factory.py 개선

### 4.1 누락된 연산자 매핑 ✅

**문제점**:
- `DivideIntegerOperation`이 매핑에 없음
- `//` 연산자가 지원되지 않음

**개선 내용**:
- `DivideIntegerOperation` import 추가
- `//` 연산자 매핑 추가
- `_operations` 딕셔너리에 `"//": DivideIntegerOperation` 추가

**변경 파일**: `src/operations/factory.py`

**개선 효과**:
- 정수 나눗셈 연산자 지원
- 연산자 지원 목록 완성

---

### 4.2 하드코딩된 매핑 ✅

**문제점**:
- 연산자와 클래스 매핑이 하드코딩됨
- 새로운 연산자 추가 시 코드 수정 필요

**개선 내용**:
- 클래스 docstring에 매핑 관리 방법 명시
- `_operations` 딕셔너리에 주석 추가로 각 연산자 그룹 구분
- 새로운 연산자 추가 방법 문서화

**변경 파일**: `src/operations/factory.py`

**개선 효과**:
- 매핑 구조 명확화
- 새로운 연산자 추가 가이드 제공

---

## 5. 전역적인 문제 개선

### 5.1 sys.path 조작 ✅

**문제점**:
- 런타임에 `sys.path`를 조작하는 것은 좋지 않은 관행
- 모듈 경로 문제의 근본 원인을 해결하지 않음

**개선 내용**:
- `setup.py` 파일 생성
- 패키지 설치 스크립트 작성
- `calculator.py`에서 sys.path 조작 제거
- `calculator_ui.py`에서 sys.path 조작 제거
- entry_points 설정으로 명령어 등록:
  - `calculator`: 콘솔 버전
  - `calculator-gui`: GUI 버전

**변경 파일**: 
- `setup.py` (신규 생성)
- `src/calculator.py`
- `src/calculator_ui.py`

**개선 효과**:
- 표준 Python 패키지 구조 준수
- `pip install -e .`로 개발 모드 설치 가능
- sys.path 조작 불필요

---

### 5.2 타입 힌팅 불일치 ✅

**문제점**:
- 일부 메서드는 타입 힌팅이 있고, 일부는 없음
- 일관성 부족

**개선 내용**:
- `calculator.py`의 `main()` 함수에 `-> None` 타입 힌팅 추가
- `calculator_ui.py`의 `main()` 함수에 `-> None` 타입 힌팅 추가
- 모든 public 메서드에 타입 힌팅 적용

**변경 파일**: 
- `src/calculator.py`
- `src/calculator_ui.py`

**개선 효과**:
- 타입 힌팅 일관성 향상
- IDE 자동완성 및 타입 체크 개선

---

### 5.3 문서화 불일치 ✅

**문제점**:
- 일부 메서드는 상세한 docstring이 있고, 일부는 없음

**개선 내용**:
- `calculator.py`의 `main()` 함수에 docstring 추가
- `calculator_ui.py`의 `main()` 함수에 docstring 추가
- 모든 public 메서드에 docstring 적용

**변경 파일**: 
- `src/calculator.py`
- `src/calculator_ui.py`

**개선 효과**:
- 문서화 일관성 향상
- 코드 이해도 향상

---

## 개선 통계

### 완료된 개선 항목

| 카테고리 | 항목 수 | 완료 수 | 완료율 |
|---------|--------|--------|--------|
| calculator_ui.py | 6 | 6 | 100% |
| calculator_controller.py | 6 | 6 | 100% |
| arithmetic.py | 2 | 1 | 50%* |
| operations/factory.py | 2 | 2 | 100% |
| 전역적인 문제 | 3 | 3 | 100% |
| **합계** | **19** | **18** | **95%** |

*arithmetic.py의 Wrapper 클래스는 의도적 설계로 유지

---

## 개선 효과

### 코드 품질
- ✅ **가독성 향상**: 긴 메서드 분리로 이해하기 쉬워짐
- ✅ **유지보수성 향상**: 중복 코드 제거로 수정 포인트 감소
- ✅ **확장성 향상**: 상수 분리로 변경이 쉬워짐
- ✅ **일관성 향상**: 타입 힌팅 및 문서화 통일

### 테스트 용이성
- ✅ **단위 테스트 용이**: 작은 메서드로 테스트하기 쉬워짐
- ✅ **Mock 객체 사용 용이**: 의존성이 명확해짐

### 성능
- ✅ **메모리 사용 감소**: Lazy Initialization으로 불필요한 객체 생성 방지
- ✅ **초기화 시간 단축**: 필요한 객체만 생성

### 프로젝트 구조
- ✅ **표준 패키지 구조**: setup.py로 표준 Python 패키지 구조 준수
- ✅ **설치 용이**: pip install로 패키지 설치 가능

---

## 검증 결과

### 테스트 통과
- ✅ 모든 테스트 통과 (40/40)
- ✅ `test_arithmetic.py`: 10개 테스트 통과
- ✅ `test_calculator_controller.py`: 30개 테스트 통과

### Linter 검사
- ✅ Linter 오류 없음
- ✅ 타입 힌팅 일관성 확인

### 기능 검증
- ✅ 계산기 기능 정상 동작
- ✅ GUI 애플리케이션 정상 실행
- ✅ 콘솔 애플리케이션 정상 실행

---

## 결론

리팩토링 코드 스멜 분석에서 식별된 19개 항목 중 18개를 개선 완료했습니다. (1개는 의도적 설계로 유지)

주요 개선 사항:
1. **중복 코드 제거**: 버튼 생성, 에러 처리 로직 통합
2. **매직 값 제거**: 모든 하드코딩된 값을 상수로 분리
3. **긴 메서드 분리**: 단일 책임 원칙 준수
4. **에러 처리 개선**: 중복 제거 및 일관성 향상
5. **프로젝트 구조 개선**: 표준 Python 패키지 구조 준수

코드 품질이 크게 향상되었으며, 유지보수성과 확장성이 개선되었습니다.

