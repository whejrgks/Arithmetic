# 코드 스멜 분석 (최신)

## 개요

현재 코드베이스의 코드 스멜을 분석한 결과입니다. 이전 개선 작업 이후에도 남아있는 코드 스멜과 새로 발견된 코드 스멜을 정리했습니다.

**분석 일자**: 2025년
**분석 범위**: `src/` 디렉토리 전체

---

## 1. calculator_ui.py

### 1.1 긴 if-elif 체인 (Long If-Elif Chain)

**위치**: `_connect_button_signal()` 메서드 (212-237줄)

**문제점**:
- 버튼 타입별 처리를 if-elif 체인으로 구현
- 새로운 버튼 타입 추가 시 메서드 수정 필요
- 가독성 저하

**예시**:
```python
def _connect_button_signal(self, button: QPushButton, text: str) -> None:
    if text.isdigit():
        button.clicked.connect(lambda checked, d=text: self._on_number_clicked(d))
    elif text in ("+", "−", "×", "/"):
        button.clicked.connect(lambda checked, op=text: self._on_operator_clicked(op))
    elif text == "=":
        button.clicked.connect(self._on_equals_clicked)
    elif text == "+/−":
        button.clicked.connect(self._on_toggle_sign_clicked)
    elif text == ".":
        button.clicked.connect(self._on_decimal_clicked)
    elif text == "Clear":
        button.clicked.connect(self._on_clear_clicked)
```

**개선 방안**:
- 딕셔너리 기반 매핑 사용
- Strategy 패턴 또는 Command 패턴 적용

**우선순위**: 중간

---

### 1.2 Lambda 클로저 문제 (Lambda Closure Issue)

**위치**: `_connect_button_signal()` 메서드, `_setup_shortcuts()` 메서드

**문제점**:
- Lambda 함수에서 변수 캡처 시 기본값 사용 (`lambda checked, d=text`)
- 루프 내에서 lambda 사용 시 예상치 못한 동작 가능성

**예시**:
```python
# _connect_button_signal에서
button.clicked.connect(lambda checked, d=text: self._on_number_clicked(d))

# _setup_shortcuts에서
for i in range(10):
    shortcut.activated.connect(lambda digit=str(i): self._on_number_clicked(digit))
```

**개선 방안**:
- `functools.partial` 사용
- 또는 명시적 함수 생성

**우선순위**: 낮음 (현재는 기본값으로 해결되어 있음)

---

### 1.3 Lazy Import (지연 Import)

**위치**: `_setup_shortcuts()` 메서드 (241줄)

**문제점**:
- 메서드 내부에서 `from PyQt5.QtWidgets import QShortcut` import
- 모듈 레벨 import와 일관성 부족

**예시**:
```python
def _setup_shortcuts(self) -> None:
    """키보드 단축키 설정"""
    from PyQt5.QtWidgets import QShortcut  # 메서드 내부 import
```

**개선 방안**:
- 모듈 레벨로 import 이동
- 순환 참조 문제가 없다면 모듈 상단으로 이동

**우선순위**: 낮음

---

### 1.4 하드코딩된 버튼 레이아웃

**위치**: `_create_button_grid()` 메서드 (103-121줄)

**문제점**:
- 버튼 레이아웃이 튜플 리스트로 하드코딩됨
- 버튼 추가/제거 시 코드 수정 필요

**예시**:
```python
buttons = [
    (0, 0, "7", "number"),
    (0, 1, "8", "number"),
    # ... 하드코딩된 레이아웃
]
```

**개선 방안**:
- 설정 파일 또는 상수로 분리
- 또는 레이아웃 빌더 클래스 생성

**우선순위**: 낮음 (현재 구조도 충분히 작동)

---

## 2. calculator_controller.py

### 2.1 복잡한 조건문 (Complex Conditional)

**위치**: `toggle_sign()` 메서드 (173줄)

**문제점**:
- 복합 조건문이 가독성을 저하시킴

**예시**:
```python
if self._current_value != self.INITIAL_VALUE and self._current_value != "0.":
```

**개선 방안**:
- 헬퍼 메서드로 분리: `_is_editable_value()`

**우선순위**: 낮음

---

### 2.2 문자열 조작 복잡도

**위치**: `_format_result()` 메서드 (253-278줄)

**문제점**:
- 문자열 포맷팅 로직이 복잡함
- 여러 단계의 문자열 조작

**예시**:
```python
formatted = str(result).rstrip('0').rstrip('.')
if not formatted or formatted == "-":
    return self.INITIAL_VALUE
```

**개선 방안**:
- 별도 포맷터 클래스 생성
- 또는 더 명확한 로직으로 리팩토링

**우선순위**: 낮음 (현재 로직은 정상 작동)

---

### 2.3 예외 처리 범위가 넓음 (Broad Exception Handling)

**위치**: `calculate()` 메서드 (154줄)

**문제점**:
- `except Exception:`으로 모든 예외를 포괄
- 구체적인 예외 타입을 알 수 없음

**예시**:
```python
except ArithmeticError:
    self._set_error_state(reset_operation=True)
except Exception:  # 너무 넓은 예외 처리
    self._set_error_state()
```

**개선 방안**:
- 구체적인 예외 타입 명시
- 로깅 추가 (선택사항)

**우선순위**: 낮음

---

## 3. arithmetic.py

### 3.1 중복된 패턴 (Duplicate Pattern)

**위치**: `_get_*_operation()` 메서드들 (32-60줄)

**문제점**:
- 동일한 패턴의 메서드가 반복됨
- Lazy Initialization 로직이 중복

**예시**:
```python
def _get_add_operation(self) -> AddOperation:
    if self._add_operation is None:
        self._add_operation = AddOperation()
    return self._add_operation

def _get_subtract_operation(self) -> SubtractOperation:
    if self._subtract_operation is None:
        self._subtract_operation = SubtractOperation()
    return self._subtract_operation
# ... 동일한 패턴 반복
```

**개선 방안**:
- 제네릭 헬퍼 메서드 생성
- 또는 딕셔너리 기반 관리

**우선순위**: 낮음 (현재 구조도 충분히 작동)

---

### 3.2 타입 힌팅 불일치

**위치**: `__init__()` 메서드 (22줄)

**문제점**:
- `__init__()` 메서드에 반환 타입 힌팅이 없음 (일관성)

**예시**:
```python
def __init__(self):  # -> None이 없음
```

**개선 방안**:
- `-> None` 추가

**우선순위**: 매우 낮음

---

## 4. operations/factory.py

### 4.1 하드코딩된 매핑 (이미 개선됨)

**상태**: ✅ 개선 완료
- 주석 추가로 구조 명확화
- docstring으로 사용법 문서화

---

## 5. 전역적인 문제

### 5.1 sys.path 조작 (조건부로 개선됨)

**위치**: `calculator_ui.py`, `calculator.py`

**상태**: ✅ 부분 개선
- 조건부 sys.path 조작으로 직접 실행 지원
- 패키지 설치 시에는 작동하지 않음

**개선 방안**:
- setup.py 사용 권장
- 또는 프로젝트 구조 개선

**우선순위**: 낮음 (현재 방식도 작동)

---

### 5.2 타입 힌팅 일관성

**상태**: ✅ 대부분 개선 완료
- 모든 public 메서드에 타입 힌팅 추가
- 일부 private 메서드에도 타입 힌팅 있음

---

### 5.3 문서화 일관성

**상태**: ✅ 대부분 개선 완료
- 모든 public 메서드에 docstring 추가
- 일부 private 메서드에도 docstring 있음

---

## 우선순위별 개선 권장사항

### 높은 우선순위 (즉시 개선 권장)

**없음** - 현재 코드는 전반적으로 잘 구조화되어 있음

### 중간 우선순위 (점진적 개선)

1. **긴 if-elif 체인 개선** (`calculator_ui.py`의 `_connect_button_signal`)
   - 딕셔너리 기반 매핑으로 변경
   - 가독성 및 확장성 향상

### 낮은 우선순위 (선택적 개선)

2. **중복된 패턴 개선** (`arithmetic.py`의 `_get_*_operation` 메서드)
   - 제네릭 헬퍼 메서드로 통합 가능
   - 하지만 현재 구조도 충분히 작동

3. **Lazy Import 개선** (`calculator_ui.py`의 `_setup_shortcuts`)
   - 모듈 레벨 import로 이동
   - 순환 참조 문제 확인 필요

4. **복잡한 조건문 개선** (`calculator_controller.py`의 `toggle_sign`)
   - 헬퍼 메서드로 분리
   - 가독성 향상

---

## 개선 효과 예상

### 코드 품질
- **가독성 향상**: if-elif 체인을 딕셔너리로 변경 시 가독성 향상
- **확장성 향상**: 새로운 버튼 타입 추가 시 코드 수정 최소화
- **일관성 향상**: 중복 패턴 제거로 코드 일관성 향상

### 유지보수성
- **수정 포인트 감소**: 중복 패턴 제거로 수정 포인트 감소
- **테스트 용이**: 작은 메서드로 분리 시 테스트 용이

---

## 결론

현재 코드베이스는 전반적으로 잘 구조화되어 있으며, 대부분의 주요 코드 스멜이 개선되었습니다. 

남아있는 코드 스멜들은 대부분:
1. **낮은 우선순위**: 즉시 개선이 필요하지 않음
2. **선택적 개선**: 현재 구조도 충분히 작동
3. **미미한 영향**: 코드 품질에 큰 영향을 주지 않음

가장 개선 가치가 있는 항목은 `calculator_ui.py`의 긴 if-elif 체인을 딕셔너리 기반 매핑으로 변경하는 것입니다. 이는 가독성과 확장성을 크게 향상시킬 수 있습니다.

---

## 통계

### 코드 스멜 현황

| 카테고리 | 발견된 스멜 | 높은 우선순위 | 중간 우선순위 | 낮은 우선순위 |
|---------|------------|-------------|-------------|-------------|
| calculator_ui.py | 4 | 0 | 1 | 3 |
| calculator_controller.py | 3 | 0 | 0 | 3 |
| arithmetic.py | 2 | 0 | 0 | 2 |
| operations/factory.py | 0 | 0 | 0 | 0 |
| 전역적인 문제 | 1 | 0 | 0 | 1 |
| **합계** | **10** | **0** | **1** | **9** |

### 개선 완료율

- **이전 개선 작업**: 18/19 항목 완료 (95%)
- **현재 상태**: 추가 개선 가능 항목 10개 (모두 낮은 우선순위)
- **전체 코드 품질**: 우수 ✅

