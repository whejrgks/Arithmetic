# 2단계: 비즈니스 로직 리팩토링 진행 내용

## 개요

2단계에서는 연산자 전략 패턴을 완성하고, Arithmetic 클래스를 Strategy 패턴을 사용하도록 리팩토링하여 Open/Closed 원칙을 준수하고 if-elif 체인을 제거했습니다.

---

## 2.1 연산자 전략 패턴 구현

### 목적
- Open/Closed 원칙 준수
- if-elif 체인 제거
- 새로운 연산자 추가 시 기존 코드 수정 없이 확장 가능

### 구현 내용

#### 1. 정수 나눗셈 전략 추가
**파일**: `src/operations/divide_integer.py`

기존 `DivideOperation`은 소수점 포함 나눗셈(`/`)을 처리하지만, `Arithmetic.divide()`는 정수 나눗셈(`//`)을 사용하므로 별도의 전략 클래스가 필요했습니다.

```python
class DivideIntegerOperation(OperationStrategy):
    """정수 나눗셈 연산 전략 클래스 (소수점 버림)"""
    
    def execute(self, a, b):
        if b == 0:
            raise ArithmeticError("Division by zero")
        return a // b
    
    def get_symbol(self) -> str:
        return "//"
```

#### 2. 모듈 초기화 업데이트
**파일**: `src/operations/__init__.py`

새로 추가된 `DivideIntegerOperation`을 모듈에 포함시켰습니다.

```python
from src.operations.divide_integer import DivideIntegerOperation

__all__ = [
    'OperationStrategy',
    'AddOperation',
    'SubtractOperation',
    'MultiplyOperation',
    'DivideOperation',
    'DivideIntegerOperation',  # 새로 추가
    'OperationFactory',
]
```

### 완료 상태
- ✅ `OperationStrategy` 추상 클래스 생성 (1단계에서 완료)
- ✅ 각 연산자별 전략 클래스 구현 (1단계에서 완료)
  - `AddOperation`
  - `SubtractOperation`
  - `MultiplyOperation`
  - `DivideOperation` (소수점 포함)
  - `DivideIntegerOperation` (정수 나눗셈) ← **2단계에서 추가**
- ✅ `OperationFactory`로 연산자 문자열 → 전략 객체 매핑 (1단계에서 완료)

---

## 2.2 Arithmetic 클래스 개선

### 목적
- Strategy 패턴을 사용하도록 리팩토링
- 기존 API 유지 (하위 호환성 보장)
- 내부적으로 Strategy 패턴 활용

### 리팩토링 전

```python
class Arithmetic:
    """사칙연산 클래스"""
    
    def add(self, a, b):
        """덧셈 연산"""
        return a + b
    
    def subtract(self, a, b):
        """뺄셈 연산"""
        return a - b
    
    # ... 각 메서드가 독립적으로 구현
```

**문제점**:
- 각 메서드가 독립적으로 구현되어 있음
- Strategy 패턴을 활용하지 않음
- 코드 중복 가능성

### 리팩토링 후

```python
class Arithmetic:
    """
    사칙연산 클래스
    Strategy 패턴을 사용하여 연산 로직을 분리합니다.
    기존 API는 유지하면서 내부적으로 Strategy 패턴을 활용합니다.
    """
    
    def __init__(self):
        """Arithmetic 클래스 초기화"""
        # Strategy 객체들을 미리 생성하여 재사용
        self._add_operation = AddOperation()
        self._subtract_operation = SubtractOperation()
        self._multiply_operation = MultiplyOperation()
        self._divide_operation = DivideOperation()
        self._divide_integer_operation = DivideIntegerOperation()
    
    def add(self, a, b):
        """덧셈 연산"""
        return self._add_operation.execute(a, b)
    
    def subtract(self, a, b):
        """뺄셈 연산"""
        return self._subtract_operation.execute(a, b)
    
    # ... 모든 메서드가 Strategy를 사용
```

**개선 사항**:
- ✅ Strategy 패턴 활용
- ✅ 기존 API 유지 (하위 호환성 보장)
- ✅ 타입 힌팅 추가
- ✅ 코드 재사용성 향상
- ✅ 유지보수성 개선

### 변경된 파일
- `src/arithmetic.py` - Strategy 패턴 적용

---

## 2.3 CalculatorController 개선

### 목적
- if-elif 체인 제거
- Strategy 패턴 직접 활용

### 리팩토링 전

```python
# Arithmetic 클래스를 사용하여 연산 실행
if self._operator == "+":
    result = self._arithmetic.add(self._previous_value, current)
elif self._operator == "-":
    result = self._arithmetic.subtract(self._previous_value, current)
elif self._operator in ("×", "*"):
    result = self._arithmetic.multiply(self._previous_value, current)
elif self._operator in ("/", "÷"):
    result = self._arithmetic.divide_quotient(self._previous_value, current)
else:
    result = operation.execute(self._previous_value, current)
```

**문제점**:
- 긴 if-elif 체인
- 새로운 연산자 추가 시 코드 수정 필요
- Open/Closed 원칙 위반

### 리팩토링 후

```python
# Strategy 패턴을 사용하여 연산 실행 (if-elif 체인 제거)
# Arithmetic 클래스도 내부적으로 Strategy 패턴을 사용하므로
# 일관성 있게 Strategy를 직접 사용
result = operation.execute(self._previous_value, current)
```

**개선 사항**:
- ✅ if-elif 체인 완전 제거
- ✅ Strategy 패턴 직접 활용
- ✅ 코드 간결화
- ✅ 확장성 향상

### 변경된 파일
- `src/calculator_controller.py` - calculate() 메서드 개선

---

## 테스트 결과

### 기존 테스트 통과 확인

```bash
python -m pytest tests/test_arithmetic.py -v
```

**결과**: ✅ 모든 테스트 통과 (10/10)

```
tests/test_arithmetic.py::TestArithmetic::test_add_1_plus_10 PASSED
tests/test_arithmetic.py::TestArithmetic::test_add_0_plus_1 PASSED
tests/test_arithmetic.py::TestArithmetic::test_add_negative_numbers PASSED
tests/test_arithmetic.py::TestArithmetic::test_subtract_5_minus_2 PASSED
tests/test_arithmetic.py::TestArithmetic::test_multiply_negative_numbers PASSED
tests/test_arithmetic.py::TestArithmetic::test_multiply_by_zero PASSED
tests/test_arithmetic.py::TestArithmetic::test_divide_integer_5_by_2 PASSED
tests/test_arithmetic.py::TestArithmetic::test_divide_quotient_5_by_2 PASSED
tests/test_arithmetic.py::TestArithmetic::test_divide_negative_10_by_2 PASSED
tests/test_arithmetic.py::TestArithmetic::test_divide_by_zero_exception PASSED

============================= 10 passed in 0.07s ==============================
```

### 하위 호환성 확인
- ✅ 기존 `Arithmetic` 클래스의 모든 메서드가 정상 작동
- ✅ 기존 테스트 코드 수정 불필요
- ✅ 기존 API 완전 유지

---

## 생성/수정된 파일 목록

### 새로 생성된 파일
1. `src/operations/divide_integer.py` - 정수 나눗셈 전략 클래스

### 수정된 파일
1. `src/arithmetic.py` - Strategy 패턴 적용
2. `src/operations/__init__.py` - DivideIntegerOperation 추가
3. `src/calculator_controller.py` - if-elif 체인 제거

---

## SOLID 원칙 준수 확인

### ✅ Open/Closed 원칙
- 새로운 연산자 추가 시 기존 코드 수정 없이 `OperationStrategy`를 상속받아 구현만 하면 됨
- `OperationFactory`에 등록만 하면 자동으로 사용 가능

### ✅ Single Responsibility 원칙
- 각 Operation 클래스는 하나의 연산만 담당
- Arithmetic 클래스는 Strategy를 조합하여 사용하는 역할만 담당

### ✅ Dependency Inversion 원칙
- Arithmetic 클래스가 구체적인 구현이 아닌 Strategy 인터페이스에 의존
- CalculatorController도 Strategy 인터페이스에 의존

---

## 개선 효과

### 코드 품질
- **if-elif 체인 제거**: CalculatorController의 calculate() 메서드에서 완전 제거
- **코드 중복 감소**: Strategy 패턴으로 연산 로직 재사용
- **가독성 향상**: 각 연산이 명확하게 분리됨

### 유지보수성
- **확장성**: 새로운 연산자 추가가 매우 쉬움
- **테스트 용이성**: 각 Strategy를 독립적으로 테스트 가능
- **하위 호환성**: 기존 API 완전 유지

### 아키텍처
- **일관성**: 모든 연산이 동일한 패턴으로 처리됨
- **모듈화**: 연산 로직이 명확하게 분리됨

---

## 다음 단계

2단계가 완료되었으므로 다음 단계로 진행할 수 있습니다:

- **3단계**: 컨트롤러 계층 구현 (이미 1단계에서 기본 구조 완료)
- **4단계**: PyQt GUI 구현 (이미 1단계에서 기본 구조 완료)
- **5단계**: 리소스 및 설정 분리
- **6단계**: 예외 처리 및 에러 핸들링
- **7단계**: 테스트 가능성 개선

---

## 요약

✅ **2.1 연산자 전략 패턴 구현**: 완료
- DivideIntegerOperation 추가
- 모든 연산자 전략 클래스 완성

✅ **2.2 Arithmetic 클래스 개선**: 완료
- Strategy 패턴 적용
- 기존 API 유지
- 타입 힌팅 추가

✅ **2.3 CalculatorController 개선**: 완료
- if-elif 체인 제거
- Strategy 패턴 직접 활용

✅ **테스트 통과**: 모든 테스트 통과 (10/10)
✅ **하위 호환성**: 기존 API 완전 유지

