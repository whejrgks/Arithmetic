# 리팩토링 코드 스멜 분석

## 개요

리팩토링된 코드를 분석하여 발견된 코드 스멜(code smell)을 정리했습니다. 각 스멜의 위치, 문제점, 개선 방안을 제시합니다.

---

## 1. calculator_ui.py

### 1.1 중복 코드 (Duplicate Code)

**위치**: `_init_ui()` 메서드 (112-175줄)

**문제점**:
- 버튼 스타일 설정 코드가 반복됨
- 버튼 생성 로직이 유사하게 반복됨

**예시**:
```python
# 나눗셈 버튼 (104-109줄)
divide_button = QPushButton("/")
divide_button.setFont(QFont("Arial", 14))
divide_button.setMinimumHeight(50)
divide_button.setStyleSheet("background-color: #4a4a4a; color: white;")

# Clear 버튼 (166-175줄)
clear_button = QPushButton("Clear")
clear_button.setFont(QFont("Arial", 14))
clear_button.setMinimumHeight(50)
clear_button.setStyleSheet("background-color: #d13438; color: white; ...")
```

**개선 방안**:
- 버튼 생성 헬퍼 메서드 추출
- 스타일을 상수로 분리

---

### 1.2 긴 메서드 (Long Method)

**위치**: `_init_ui()` 메서드 (35-175줄, 약 140줄)

**문제점**:
- 단일 메서드가 너무 많은 책임을 가짐
- UI 초기화, 버튼 생성, 스타일 적용, 이벤트 연결이 모두 한 메서드에 있음

**개선 방안**:
- 메서드 분리:
  - `_create_display()`
  - `_create_button_grid()`
  - `_create_button()`
  - `_connect_button_signals()`

---

### 1.3 매직 스트링 (Magic String)

**위치**: 전체 파일

**문제점**:
- 하드코딩된 색상 코드, 폰트 이름, 스타일 문자열

**예시**:
```python
"background-color: #4a4a4a; color: white;"
"background-color: #0078d4; color: white;"
"background-color: #2d2d2d; color: white;"
"background-color: #d13438; color: white;"
"background-color: #1e1e1e; color: white;"
QFont("Arial", 24, QFont.Bold)
QFont("Arial", 14)
```

**개선 방안**:
- 상수 클래스 또는 설정 파일로 분리
- `resources/styles.py` 또는 QSS 파일 사용

---

### 1.4 중복 호출 (Duplicate Call)

**위치**: `__init__()` 메서드 (32-33줄)

**문제점**:
```python
self._setup_shortcuts()  # 키보드 단축키 설정
self._setup_shortcuts()  # 키보드 단축키 설정  <- 중복!
```

**개선 방안**:
- 중복된 호출 제거

---

### 1.5 하드코딩된 값 (Hard-coded Values)

**위치**: 여러 곳

**문제점**:
- 윈도우 크기: `setFixedSize(320, 450)`
- 폰트 크기: `24`, `14`
- 버튼 높이: `50`
- 매직 넘버들이 코드에 직접 포함

**개선 방안**:
- 상수로 정의하거나 설정 파일로 분리

---

### 1.6 Feature Envy

**위치**: `_on_operator_clicked()` 메서드 (195-205줄)

**문제점**:
- UI 클래스가 컨트롤러의 내부 동작을 너무 많이 알고 있음
- "−" 기호를 "-"로 변환하는 로직이 UI에 있음

**개선 방안**:
- 기호 변환 로직을 컨트롤러로 이동
- 또는 UI에서 표시용 기호와 내부 기호를 분리

---

## 2. calculator_controller.py

### 2.1 매직 스트링 (Magic String)

**위치**: 여러 메서드

**문제점**:
- "Error" 문자열이 하드코딩됨 (여러 곳에서 사용)

**예시**:
```python
if self._current_value == "Error":  # 58, 83, 104, 114, 136, 148, 163, 169, 185줄
    ...
self._current_value = "Error"  # 60, 96, 123, 136, 148, 163, 169줄
```

**개선 방안**:
- 상수로 정의: `ERROR_STATE = "Error"`

---

### 2.2 중복 코드 (Duplicate Code)

**위치**: 여러 메서드

**문제점**:
- 에러 상태 체크 및 처리 로직이 반복됨

**예시**:
```python
# input_number()에서
if self._current_value == "Error":
    self.clear()
    self._current_value = digit
    self._update_display()
    return

# input_decimal()에서
if self._current_value == "Error":
    self.clear()
    self._current_value = "0."
    self._update_display()
    return
```

**개선 방안**:
- `_is_error_state()`, `_handle_error_state()` 헬퍼 메서드 추출

---

### 2.3 매직 넘버 (Magic Number)

**위치**: `calculate()` 메서드 (147줄)

**문제점**:
```python
if abs(result) > 1e15:  # 1e15가 무엇을 의미하는지 불명확
    self._current_value = "Error"
```

**개선 방안**:
- 상수로 정의: `MAX_DISPLAY_VALUE = 1e15`

---

### 2.4 긴 메서드 (Long Method)

**위치**: `calculate()` 메서드 (126-170줄, 약 45줄)

**문제점**:
- 계산, 결과 포맷팅, 에러 처리, 상태 업데이트가 모두 한 메서드에 있음

**개선 방안**:
- 메서드 분리:
  - `_format_result()`
  - `_handle_calculation_error()`

---

### 2.5 예외 처리 중복 (Exception Handling Duplication)

**위치**: `calculate()` 메서드 (162-170줄)

**문제점**:
- 에러 상태 설정 로직이 중복됨

**예시**:
```python
except ArithmeticError as e:
    self._current_value = "Error"
    self._operator = None
    self._previous_value = None
    self._waiting_for_operand = True
    self._update_display()
except Exception as e:
    self._current_value = "Error"
    self._update_display()
```

**개선 방안**:
- `_set_error_state()` 메서드 추출

---

### 2.6 결과 포맷팅 로직 복잡도

**위치**: `calculate()` 메서드 (145-155줄)

**문제점**:
- 결과를 문자열로 변환하는 로직이 복잡함
- 소수점 처리 로직이 까다로움

**개선 방안**:
- `_format_result()` 메서드로 분리
- 또는 별도 포맷터 클래스 생성

---

## 3. arithmetic.py

### 3.1 불필요한 객체 생성 (Unnecessary Object Creation)

**위치**: `__init__()` 메서드 (20-27줄)

**문제점**:
- Strategy 객체를 미리 생성하여 저장
- 사용하지 않을 수도 있는 객체를 미리 생성

**예시**:
```python
self._add_operation = AddOperation()
self._subtract_operation = SubtractOperation()
# ... 모든 연산 객체를 미리 생성
```

**개선 방안**:
- 필요할 때 생성 (Lazy Initialization)
- 또는 Factory 패턴 사용

---

### 3.2 Wrapper 클래스 (Thin Wrapper)

**위치**: 전체 클래스

**문제점**:
- Arithmetic 클래스가 단순히 Strategy를 호출하는 래퍼 역할만 함
- 추가적인 가치가 거의 없음

**개선 방안**:
- Strategy를 직접 사용하도록 변경
- 또는 Arithmetic에 추가 비즈니스 로직 추가

---

## 4. operations/factory.py

### 4.1 누락된 연산자 매핑

**위치**: `_operations` 딕셔너리 (16-23줄)

**문제점**:
- `DivideIntegerOperation`이 매핑에 없음
- `//` 연산자가 지원되지 않음

**개선 방안**:
- `DivideIntegerOperation` 추가

---

### 4.2 하드코딩된 매핑

**위치**: `_operations` 딕셔너리

**문제점**:
- 연산자와 클래스 매핑이 하드코딩됨
- 새로운 연산자 추가 시 코드 수정 필요

**개선 방안**:
- 설정 파일로 분리 (선택사항)
- 또는 현재 구조 유지 (Strategy 패턴의 장점)

---

## 5. 전역적인 문제

### 5.1 sys.path 조작

**위치**: `calculator_ui.py` (8-11줄), `calculator.py` (7-10줄)

**문제점**:
- 런타임에 `sys.path`를 조작하는 것은 좋지 않은 관행
- 모듈 경로 문제의 근본 원인을 해결하지 않음

**개선 방안**:
- 프로젝트 구조 개선
- 또는 `PYTHONPATH` 환경 변수 사용
- 또는 `setup.py`로 패키지 설치

---

### 5.2 타입 힌팅 불일치

**위치**: 여러 파일

**문제점**:
- 일부 메서드는 타입 힌팅이 있고, 일부는 없음
- 일관성 부족

**개선 방안**:
- 모든 메서드에 타입 힌팅 추가

---

### 5.3 문서화 불일치

**위치**: 여러 파일

**문제점**:
- 일부 메서드는 상세한 docstring이 있고, 일부는 없음

**개선 방안**:
- 모든 public 메서드에 docstring 추가

---

## 우선순위별 개선 권장사항

### 높은 우선순위 (즉시 개선)

1. **중복 호출 제거** (`calculator_ui.py` 33줄)
2. **매직 스트링 상수화** ("Error" 문자열)
3. **에러 처리 로직 중복 제거** (`calculator_controller.py`)

### 중간 우선순위 (점진적 개선)

4. **긴 메서드 분리** (`_init_ui()`, `calculate()`)
5. **버튼 생성 로직 리팩토링** (`calculator_ui.py`)
6. **스타일 상수 분리** (5단계에서 처리 예정)

### 낮은 우선순위 (선택적 개선)

7. **Arithmetic 클래스 리팩토링** (현재 구조도 충분히 작동)
8. **sys.path 조작 개선** (프로젝트 구조 개선 필요)
9. **타입 힌팅 완성도 향상**

---

## 개선 효과 예상

### 코드 품질
- **가독성 향상**: 긴 메서드 분리로 이해하기 쉬워짐
- **유지보수성 향상**: 중복 코드 제거로 수정 포인트 감소
- **확장성 향상**: 상수 분리로 변경이 쉬워짐

### 테스트 용이성
- **단위 테스트 용이**: 작은 메서드로 테스트하기 쉬워짐
- **Mock 객체 사용 용이**: 의존성이 명확해짐

### 성능
- **최소한의 영향**: 대부분의 개선은 성능에 큰 영향 없음
- **Lazy Initialization**: Arithmetic 클래스 개선 시 메모리 사용 감소 가능

---

## 결론

리팩토링된 코드는 전반적으로 잘 구조화되어 있지만, 여전히 개선할 수 있는 코드 스멜들이 존재합니다. 특히:

1. **중복 코드**: 여러 곳에서 반복되는 로직
2. **매직 값**: 하드코딩된 문자열과 숫자
3. **긴 메서드**: 여러 책임을 가진 메서드들
4. **에러 처리**: 중복된 에러 상태 관리 로직

이러한 스멜들을 개선하면 코드 품질이 더욱 향상될 것입니다.

